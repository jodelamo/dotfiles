name: search-service
root: ~/Dev/search-service

windows:
  - editor: vim
  - search-service:
      layout: even-horizontal
      root: ~/Dev/ikea/search-service
      panes:
        elasticsearch: gcloud container clusters get-credentials retail-pim --region europe-north1 --project ikea-retail-pim-dev && kubectl port-forward --namespace elastic-index-service $(kubectl get pod --namespace elastic-index-service --selector="app=app" --output jsonpath='{.items[0].metadata.name}') 9200:9200
        api: GOOGLE_APPLICATION_CREDENTIALS="/Users/jolow/Library/Mobile\ Documents/com~apple~CloudDocs/Keys/GCP/ikea-retail-pim-publisher/ikea-retail-pim-publisher-dev.json" go run main.go
