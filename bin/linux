#!/bin/sh

ZSH="$(cd "$(dirname -- "$0")/.." && pwd)"
export ZSH

# Install apt packages
packages_file="$ZSH/apt/packages.txt"

echo "Installing apt packages..."
sudo apt update
grep -v '^#' "$packages_file" | grep -v '^$' | xargs sudo apt install -y --no-install-recommends
